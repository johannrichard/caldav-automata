# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Editor
!.vscode/
.vscode/*
!.vscode/launch.json
.idea/
*.swp
*.swo
*~

# Docker
.docker/

# Radicale data (when running locally without Docker)
data/

# Secrets — never commit real credentials
config/*
!config/*.example
config/*.yaml
!config/calendar.example.yaml

# Local rules — keep only the shipped example in git
rules/**
!rules/
!rules/*.example

# Environment file — contains secrets; use .env.example as the committed template
.env

# Local secret files for Docker Compose / mounted secret injection
secrets/
