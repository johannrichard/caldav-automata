# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
.docker/

# Radicale data (when running locally without Docker)
data/

# Secrets — never commit real credentials
config/htpasswd
!config/htpasswd.example
config/calendar.yaml
!config/calendar.example.yaml

# Local rules — keep only the shipped example in git
rules/**
!rules/
!rules/example.lisp.example

# Environment file — contains secrets; use .env.example as the committed template
.env
